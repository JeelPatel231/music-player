<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MusicPlayer.Base" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.MusicPlayer" parent="Theme.MusicPlayer.Base" />

    <style name="ShapeAppearanceOverlay.MaterialCardView.Cut" parent="Theme.MusicPlayer.Base">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
    </style>
</resources>